services:
  web:
    build: .
    ports:
      - "${APP_COMPOSE_PORT}:3000"
    environment:
      - NODE_ENV=production
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_HOST=db
      - DB_PORT=5432
      - DB_NAME=${DB_NAME}

volumes:
  postgres_data:
